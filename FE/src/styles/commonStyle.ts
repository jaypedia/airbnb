const shadow = `
    box-shadow: rgba(99, 99, 99, 0.2) 0px 2px 8px 0px;
`;

const ActivatedInputShadow = `
    box-shadow: rgb(0 0 0 / 25%) 0px 5px 20px 1px;
`;

const TRANSITION_EFFECT = 'all 0.5s ease';

export { shadow, ActivatedInputShadow, TRANSITION_EFFECT };
