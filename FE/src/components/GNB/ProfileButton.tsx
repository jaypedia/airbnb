import * as S from './GNB.style';

import * as I from '@/styles/icons';

const ProfileButton = () => {
  return (
    <S.ProfileButton>
      <I.Menu />
      <I.User />
    </S.ProfileButton>
  );
};

export default ProfileButton;
